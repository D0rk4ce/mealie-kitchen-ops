# ==========================================
# GENERAL SETTINGS
# ==========================================
DRY_RUN=true
SCRIPT_TO_RUN=tagger
LOG_LEVEL=INFO

# ==========================================
# MEALIE API (Used by: Parser, Cleaner)
# ==========================================
MEALIE_URL=http://your-mealie-instance:9000
MEALIE_API_TOKEN=your_api_token_here

# ==========================================
# DATABASE SETTINGS (Used by: Tagger)
# ==========================================
# Options: sqlite, postgres
DB_TYPE=sqlite

# --- SQLite (path inside the container) ---
SQLITE_PATH=/app/data/mealie.db

# --- Postgres (uncomment and fill in if DB_TYPE=postgres) ---
# POSTGRES_HOST=192.168.1.X
# POSTGRES_PORT=5432
# POSTGRES_DB=mealie_db
# POSTGRES_USER=mealie_user
# POSTGRES_PASSWORD=YOUR_DB_PASSWORD

# ==========================================
# PERFORMANCE TUNING
# ==========================================
# Number of concurrent threads for each script.
# Default of 2 is safe for most setups.
PARSER_WORKERS=2
CLEANER_WORKERS=2
