# ==========================================
# 🛡️ GENERAL SAFETY
# ==========================================
# true:  Prints what would happen, changes nothing.
# false: ACTUALLY modifies/deletes data.
DRY_RUN=true

# ==========================================
# 🚀 DOCKER LAUNCH CONFIG
# ==========================================
# Which tool should run when the container starts?
# Options: 
#   tagger  - Runs Auto-Tagger (Default)
#   parser  - Runs Batch Parser
#   cleaner - Runs Library Cleaner
#   all     - Runs Tagger -> Cleaner -> Parser in order
SCRIPT_TO_RUN=tagger

# ==========================================
# 🔗 MEALIE CONNECTION
# ==========================================
# Required for Parser and Cleaner
MEALIE_URL=http://localhost:9000
MEALIE_API_TOKEN=your_long_api_token_here

# ==========================================
# ⚙️ PERFORMANCE TUNING
# ==========================================
# How many concurrent threads to run?
# Recommended: 2 for Raspberry Pi, 5+ for Desktop/Server
PARSER_WORKERS=2
CLEANER_WORKERS=2

# ==========================================
# 🏷️ AUTO-TAGGER CONFIGURATION
# ==========================================
# Database Mode: "sqlite" (Default) or "postgres"
DB_TYPE=sqlite

# [SQLITE USERS]
# Path to the mealie.db file (inside the container)
SQLITE_PATH=/app/data/mealie.db

# [POSTGRES USERS]
# The Tagger uses standard libpq environment variables.
# These are passed directly to the psql command.
DB_NAME=mealie_db
PGHOST=192.168.1.X
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_db_password

# ==========================================
# 🔍 ADVANCED / DEBUGGING
# ==========================================
# Logging Level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
